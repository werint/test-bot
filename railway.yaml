build:
  builder: "nixpacks"

services:
  - name: postgres
    type: "postgresql"
    
  - name: discord-bot
    type: "web"
    source:
      type: "github"
    start:
      type: "worker"
      command: "python bot1.py"